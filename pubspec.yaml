name: suez_canal_assignment
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  http: ^1.2.2
  get_it: ^7.7.0
  equatable: ^2.0.5
  flutter_dotenv: ^5.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.16
  bloc_test: ^9.1.7
  build_runner:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - .env